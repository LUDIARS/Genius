{
  "name": "notification",
  "description": "Concordia chat notifications (POST /v1/chat) for failed/completed-with-errors runs, clean runs that generated questions, notify-failed logging, and public-only active-learning delivery through the dedicated Genius channel. Never emits card/document bodies or absolute paths.",
  "presetRules": [],
  "templateRules": [],
  "membership": [
    {
      "pathPattern": "/src/services/"
    },
    {
      "pathPattern": "/src/client/"
    },
    {
      "pathPattern": "/src/runtime/"
    },
    {
      "namePattern": "[Nn]otify"
    },
    {
      "namePattern": "[Cc]oncordia"
    },
    {
      "namePattern": "IngestRunContract"
    },
    {
      "namePattern": "isIngestRunSuccessful"
    },
    {
      "namePattern": "client"
    }
  ],
  "cardTemplate": "Summarise the \"notification\" domain: Concordia chat notifications (POST /v1/chat) for failed/completed-with-errors runs, clean runs that generated questions, notify-failed logging, and public-only active-learning delivery through the dedicated Genius channel. Never emits card/document bodies or absolute paths.",
  "specRefs": [
    "Genius — 自分クローン判断カード DB / Concordia 通知",
    "Genius 運用化設計 / 4. ingest 失敗の可視化と LLM フォールバック (#4)",
    "Genius — 自分クローン判断カード DB / 日次運用と Concordia Timer Delegation",
    "Genius 補完質問 (能動学習) 設計 / 3.2 Discord (public のみ、Concordia 経由)",
    "Genius 補完質問 (能動学習) 設計 / 5. cadence と運用"
  ],
  "rationale": "Outbound integration + run-contract clients (services/client/runtime) share the redaction rules and run-success contract used for external notification."
}
